[metadata]
  name = "rust-cross-versions-{{ $.Env.GitReference }}"

[global]
  plan = "ursa/network"
  case = "network"
  builder = "docker:generic"
  runner = "local:docker"
  concurrent_builds = 1

[[groups]]
  id = "service"
  instances = { count = 3 }
  [groups.build_config.build_args]
    BINARY_NAME = 'testplan_service'
